<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scut.onlinebookstore.dao.CartDao">
    <resultMap id="cartMap" type="com.scut.onlinebookstore.models.CartItem">
        <id column="userId" property="userId" />
        <id column="bookISBN" property="bookISBN" />
        <result column="count" property="count" />
    </resultMap>

    <insert id="addCart">
        INSERT INTO CartItem(userId, bookISBN, count)
        VALUES (#{0},#{1},#{2})
    </insert>
    
    <delete id="removeFromCart" >
        DELETE FROM CartItem WHERE userId=#{0} and bookISBN=#{1}
    </delete>
    
    <delete id="clearCart" parameterType="Integer" >
        DELETE FROM Book WHERE userId=#{userId}
    </delete>


</mapper>